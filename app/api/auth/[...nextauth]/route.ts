export async function GET() {
  return Response.json({ ok: false, message: "Auth routes are not used in this build." });
}

export async function POST() {
  return Response.json({ ok: false, message: "Auth routes are not used in this build." });
}
